{
  "_comment": [
    "Copy this to ~/.understudy/credentials.json, or use the UI's Settings tab,",
    "which writes the same file with owner-only permissions.",
    "Never commit a filled-in copy: the real file lives outside the repository",
    "by default, and .gitignore covers it if you move it inside.",
    "An ANTHROPIC_API_KEY environment variable takes precedence over this file."
  ],
  "api_key": "sk-ant-api03-REPLACE-ME",
  "base_url": "",
  "model": "claude-opus-5"
}
